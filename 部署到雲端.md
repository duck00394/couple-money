# 把 Couple Money 放上雲端（隨時隨地都能用）

三個免費服務各司其職：

```
GitHub（放程式碼）→ Vercel（跑 App）→ Neon（PostgreSQL 資料庫）
```

做完之後：**電腦可以關機**，你和另一半用手機瀏覽器隨時都能開。

---

## 步驟 1：安裝 GitHub Desktop

到 https://desktop.github.com 下載安裝，用你的 GitHub 帳號登入。

（用這個就不用學 git 指令。）

---

## 步驟 2：把專案推上 GitHub

1. 打開 GitHub Desktop
2. 左上角 **File → Add local repository**
3. 選你的專案資料夾（裡面有 `package.json` 那一層）
4. 它會說「這不是 git 倉庫」→ 點 **create a repository**
5. Name 填 `couple-money`，其他不用改 → **Create repository**
6. 左邊會列出所有檔案，下方 Summary 填 `first commit` → **Commit to main**
7. 上方按 **Publish repository**
8. **把「Keep this code private」取消勾選**（你要公開）→ **Publish repository**

> GitHub Desktop 會自動照 `.gitignore` 排除 `node_modules`、`.env`、`.uploads`，
> 所以你的密碼和照片**不會**被上傳。

---

## 步驟 3：在 Neon 建立資料庫

1. 到 https://neon.com → **Sign up** → 選 **Continue with GitHub**
2. 建立 Project：
   - Project name：`couple-money`
   - Region：選 **Asia Pacific (Singapore)**（離台灣最近）
   - Postgres version：預設就好
3. 建好後會出現 **Connection string**，把它複製起來，長得像：
   ```
   postgresql://neondb_owner:xxxx@ep-xxx-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
   ```
4. 在同一個畫面找 **Connection pooling** 的開關或 **Direct connection** 選項，
   再複製一份**沒有 `-pooler`** 的網址，長得像：
   ```
   postgresql://neondb_owner:xxxx@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
   ```

兩串都先貼到記事本，等一下要用。

> 差別只在主機名稱有沒有 `-pooler`。App 平常用 pooled（省連線），
> 建資料表時用 direct（不能走連線池）。

---

## 步驟 4：在 Vercel 部署

1. 到 https://vercel.com → **Sign up** → **Continue with GitHub**
2. **Add New → Project**
3. 找到 `couple-money` → **Import**
4. 先不要按 Deploy，展開 **Environment Variables**，加三個：

   | Name | Value |
   |---|---|
   | `DATABASE_URL` | 步驟 3 那串**有 `-pooler`** 的 |
   | `DATABASE_URL_UNPOOLED` | 步驟 3 那串**沒有 `-pooler`** 的 |
   | `NEXT_PUBLIC_PHOTOS_ENABLED` | `0` |

5. 按 **Deploy**，等 2～4 分鐘

部署過程會自動建立資料表（`package.json` 裡的 `vercel-build` 會跑 `prisma migrate deploy`）。

完成後 Vercel 會給你一個網址，像：

```
https://couple-money-xxxx.vercel.app
```

---

## 步驟 5：開始用

1. 手機或電腦瀏覽器打開那個網址
2. 點「註冊」→ 填暱稱、Email、密碼 → 建立帳號
3. 建立帳本
4. 到「更多」→「產生邀請碼」
5. 把邀請碼給另一半，他在自己手機註冊時輸入，就加入同一本帳本

### 加到手機主畫面（像 App 一樣）

- **iPhone**：Safari 打開 → 下方分享鍵 → 「加入主畫面」
- **Android**：Chrome 打開 → 右上角三個點 → 「加到主畫面」

---

## 這個雲端版與本機版的差別

| | 本機版 | 雲端版 |
|---|---|---|
| 在哪裡用 | 只有家裡電腦開著時 | 隨時隨地 |
| 資料存在哪 | 你的電腦 | Neon（新加坡） |
| 收據／打卡照片 | ✅ 可用 | ❌ 暫時關閉 |
| 其他所有功能 | ✅ | ✅ |

**兩邊的資料是分開的**，本機記的不會同步到雲端。建議只挑一邊當正式使用。

---

## 之後想改程式

在電腦改完 → 打開 GitHub Desktop → 填 Summary → **Commit to main** → **Push origin**。
Vercel 會自動重新部署，1～2 分鐘後手機重新整理就是新版。

---

## 想把照片功能補回來

需要把上傳改成存雲端儲存（例如 Vercel Blob）。跟我說一聲我再處理，
改好之後把 `NEXT_PUBLIC_PHOTOS_ENABLED` 拿掉或設成 `1` 就會恢復。

---

## 費用

- GitHub Public 倉庫：免費
- Vercel Hobby：免費（個人非商業用途）
- Neon Free：免費（0.5 GB，兩個人記十年也用不完）

三個都不用綁信用卡。

---

## 卡住了

| 狀況 | 處理 |
|---|---|
| Vercel 部署失敗，log 有 `P1001` 或連不上資料庫 | `DATABASE_URL_UNPOOLED` 貼錯（要用**沒有** `-pooler` 的那串） |
| 網頁打得開但註冊失敗 | 資料表沒建成功，到 Vercel 的 Deployments 看 build log |
| 第一次開很慢 | Neon 免費方案閒置後會休眠，第一個請求要等幾秒，正常 |
| 想換網址 | Vercel 專案 Settings → Domains 可以改成 `你想要的名字.vercel.app` |

部署 log 截圖給我，我看得出是哪裡。
