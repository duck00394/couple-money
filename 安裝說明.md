# Couple Money — 安裝與啟動（Windows）

跟著做，總共 7 步，第一次大約 15 分鐘。之後每次開啟只要 1 步。

---

## 步驟 1：確認有沒有 PostgreSQL

按 `Win` 鍵，輸入 **pgAdmin**。

- **有跳出「pgAdmin 4」** → 你已經裝好了，跳到步驟 2
- **沒有** → 到 https://www.postgresql.org/download/windows/ 下載安裝，
  安裝過程會要你設一組 **密碼**，請**寫下來**，後面每一步都會用到。
  其他選項全部用預設（Port 保持 5432）。

---

## 步驟 2：用 pgAdmin 建立資料庫（滑鼠點就好）

1. 打開 **pgAdmin 4**（第一次會要你設一組 master password，隨便設一組記住就好）
2. 左邊點開 **Servers** → **PostgreSQL 17**（或 16）
3. 它會問 PostgreSQL 密碼 → 輸入步驟 1 那組 → 可勾選 Save Password
4. 在 **Databases** 上按**滑鼠右鍵** → **Create** → **Database...**
5. **Database** 欄位輸入：`couple_money`
6. 按 **Save**

左邊 Databases 底下出現 `couple_money` 就成功了。

---

## 步驟 3：確認有沒有 Node.js

按 `Win` 鍵 → 輸入 **PowerShell** → 打開 → 輸入：

```
node -v
```

- 出現 `v20.x` 以上 → 有了
- 出現紅字說找不到 → 到 https://nodejs.org 下載 **LTS** 版安裝，
  裝完**關掉 PowerShell 重開**再試一次

---

## 步驟 4：解壓縮

把 `couple-money-安裝包.zip` 解壓縮到 **`C:\couple-money`**。

解完之後，`C:\couple-money` 裡面要看得到 `package.json`、`src`、`prisma` 這些東西。
如果變成 `C:\couple-money\couple-money\...`，就把裡面那層的東西移出來，或直接用裡面那層的路徑。

---

## 步驟 5：在專案資料夾打開 PowerShell

1. 用檔案總管打開 `C:\couple-money`
2. 在**上方的網址列**點一下（整條會變成可編輯）
3. 輸入 `powershell` 按 Enter

會跳出一個藍色視窗，開頭顯示 `PS C:\couple-money>` 就對了。

---

## 步驟 6：建立連線設定檔

在剛剛的 PowerShell 視窗輸入：

```
notepad .env
```

記事本會問「找不到檔案，要建立新檔嗎？」→ 按 **是**。

把下面兩行貼進去，**把 `你的密碼` 換成步驟 1 那組密碼**：

```
DATABASE_URL="postgresql://postgres:你的密碼@localhost:5432/couple_money"
DATABASE_URL_UNPOOLED="postgresql://postgres:你的密碼@localhost:5432/couple_money"
```

按 `Ctrl + S` 存檔，關掉記事本。

> 密碼裡如果有 `@` `:` `/` `%` `&` 這類符號會出問題。
> 最省事的做法是把 PostgreSQL 密碼改成純英數字。

---

## 步驟 7：安裝並啟動

在 PowerShell 依序輸入這三行（**一行一行來，等上一行跑完再輸入下一行**）：

```
npm install
```
第一次 1～3 分鐘，畫面會一直跑字，跑完會回到 `PS C:\couple-money>`。

```
npx prisma migrate deploy
```
看到 `All migrations have been successfully applied.` 就成功。

```
npm run dev
```
看到 `Ready in ...` 之後，打開瀏覽器輸入：

**http://localhost:3000**

---

## 完成！第一次使用

1. 畫面會出現登入頁 → 點「註冊」
2. 填暱稱、Email、密碼（至少 8 碼）→ 建立帳號
3. 建立帳本，輸入帳本名稱 → 建立
4. 在「更多」頁按「產生邀請碼」，把邀請碼給另一半
5. 另一半在自己手機或另一個瀏覽器（無痕視窗）註冊，輸入邀請碼加入

兩個人就在同一本帳本了。

---

## 以後每次要用

1. 檔案總管開 `C:\couple-money`
2. 網址列輸入 `powershell` 按 Enter
3. 輸入 `npm run dev`
4. 瀏覽器開 http://localhost:3000

要關掉 App：在 PowerShell 視窗按 `Ctrl + C`，或直接關掉視窗。

---

## （選做）先放一些示範資料看看畫面

如果你想先看「有很多資料時長什麼樣子」，而不是從空帳本開始：

**注意：這會把資料庫清空重來，所以只在你還沒開始記真實資料時做。**

在 PowerShell（先按 `Ctrl + C` 停掉 App）輸入：

```
npx tsx scripts/seed-demo.ts --reset
```

它會建立兩個示範帳號與一整個月的資料：

| | 帳號 | 密碼 |
|---|---|---|
| A | `demo-a@couple.local` | `demo1234` |
| B | `demo-b@couple.local` | `demo1234` |

看完之後想從乾淨的帳本開始記真實資料，再跑一次同一行指令，然後自己註冊即可。

---

## 卡住了怎麼辦

| 症狀 | 處理 |
|---|---|
| `node` 不是內部或外部命令 | Node.js 沒裝或沒重開 PowerShell |
| `npm install` 一直失敗 | 網路問題，換網路重試 |
| `prisma migrate deploy` 說連不上 | ① pgAdmin 裡確認 `couple_money` 有建立 ② `.env` 密碼打錯 ③ 密碼有特殊符號 |
| 瀏覽器打不開 / 一直轉 | 看 PowerShell 有沒有紅字；`npm run dev` 必須保持執行中 |
| 想換一個乾淨的資料庫 | 在 pgAdmin 建一個新的，然後改 `.env` 裡的資料庫名稱 |

任何一步卡住，把 **PowerShell 視窗截圖**給我，我看得出是哪一步。
