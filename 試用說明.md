# Couple Money V1 — 試用說明（Windows）

不用打指令，**雙擊三個 .bat 檔案**就能跑。

---

## 你只需要做三件事

### 第 1 件：確認電腦有這兩個東西

| 需要的東西 | 怎麼確認 | 沒有的話 |
|---|---|---|
| **Node.js 20 以上** | 開「命令提示字元」打 `node -v`，看到 `v20.x` 以上就有 | 到 <https://nodejs.org> 下載 **LTS** 版，一直按下一步 |
| **PostgreSQL 16** | 你說已經裝好了 | — |

> PostgreSQL 還要能在命令列用 `psql`。
> 如果等一下第一個 .bat 說「找不到 psql」，照它畫面上的指示把
> `C:\Program Files\PostgreSQL\16\bin` 加進系統 PATH 就好。

---

### 第 2 件：解壓縮

把 `couple-money-v1-b5d1157.zip` 解壓縮到一個好找的地方，例如：

```
C:\couple-money
```

⚠️ **不要解壓縮到 Program Files、桌面同步資料夾（OneDrive）或路徑很深的地方**，
容易出現權限或路徑太長的問題。

解開後你會看到資料夾裡有這幾個檔案：

```
1-第一次設定.bat     ← 先雙擊這個（只做一次）
2-啟動.bat           ← 以後每次都雙擊這個
3-重建示範資料.bat   ← 想把資料清空重來時才用
試用說明.md          ← 這份文件
src\ prisma\ ...     ← 程式碼
```

---

### 第 3 件：雙擊 `1-第一次設定.bat`

它會自己做完這些事：

1. 檢查 Node.js 與 PostgreSQL
2. 問你 **PostgreSQL 的使用者名稱**（直接按 Enter 就是 `postgres`）
3. 問你 **PostgreSQL 的密碼**（安裝 PostgreSQL 時你設的那個）
4. 建立一個試用專用的資料庫 `couple_money_demo`
5. 安裝套件（第一次比較久，1～3 分鐘，畫面會一直跑字，**不要關視窗**）
6. 建立資料表
7. 放入示範資料

最後看到：

```
==================================================
  設定完成！
  接下來請雙擊「2-啟動.bat」開始試用。
==================================================
```

就成功了。**這個檔案一輩子只要跑一次。**

---

## 之後每次要用：雙擊 `2-啟動.bat`

- 視窗會開始跑，約 10 秒後**自動打開瀏覽器**
- 如果沒自動打開，自己開瀏覽器輸入：**http://localhost:3000**
- 要關掉 App：在那個黑色視窗按 `Ctrl + C`，或直接關掉視窗
- ⚠️ 黑色視窗不能關，關了 App 就停了

---

## 測試帳號

| | 帳號 | 密碼 | 暱稱 |
|---|---|---|---|
| A | `demo-a@couple.local` | `demo1234` | 小安 |
| B | `demo-b@couple.local` | `demo1234` | 小森 |

**想同時體驗兩個人：**

- 一般瀏覽器視窗 → 登入 A
- 無痕視窗（`Ctrl + Shift + N`）→ 登入 B

這樣就能看到「A 記一筆 → B 那邊欠款跟著變」、「B 打卡 → A 的最近動態看得到」。

**想用手機看：**
手機跟電腦連同一個 Wi-Fi，在電腦的命令提示字元打 `ipconfig` 看 IPv4 位址，
手機瀏覽器輸入 `http://那個IP:3000`。連不上通常是 Windows 防火牆要允許 Node.js。

---

## 想把資料清空重來

雙擊 `3-重建示範資料.bat`，輸入 `y` 再按 Enter。
它只會動 `couple_money_demo` 這個試用資料庫，不會碰你其他資料庫。

---

## 建議的第一次試用流程（10～15 分鐘）

1. **登入 A**（`demo-a@couple.local` / `demo1234`）→ 看首頁：本月支出、誰欠誰、預算提醒、最近紀錄
2. **記一筆**：右上角「＋ 記一筆」→ 金額 `250` → 選「餐飲」→ 名稱「午餐」→ 帳戶選「我的・現金」
   → 看下面「怎麼分」的平分結果與「小森 要還你 …」→ 按「記下來」
3. **看分帳**：回首頁看欠款有沒有跟著變；點進剛剛那筆看明細
4. **編輯／刪除**：把金額改成 `300` 存檔，再把它刪掉（會跳確認），確認首頁數字回復
5. **搜尋／篩選**：記帳頁上方搜尋「火鍋」；再按「篩選」選類型＝支出、日期區間，看總計
6. **批次操作**：記帳頁按「選取多筆」→ 勾兩筆 → 改分類 / 加標籤
7. **看預算**：更多 →「每月預算」→ 看餐飲的已支出／剩餘／使用率（有一個是「快超過」）
8. **看統計**：底部「記帳」→「📊 統計」→ 淨支出、誰掏錢、誰負擔、分類佔比、最近 6 個月
9. **任務打卡**：底部「任務」→ 對「一起散步」按「打卡」→ 回首頁看今日獎金變多（尚未入金）
10. **目標與基金**：底部「目標」→ 看「日本旅行」進度 → 點進基金，試「入金」把獎金變成實際金額
11. **收據照片**：任一筆消費的明細頁 →「收據照片」上傳一張照片 → 點縮圖放大
12. **轉帳／退款**：記帳頁 →「🔁 轉帳」把錢從銀行搬到共同帳戶；「↩️ 退款」對「居酒屋」退一部分
13. **餘額調整**：記帳頁 →「💳 帳戶」→ 任一帳戶下面「調整餘額」
14. **活動紀錄**：用無痕視窗登入 B 做幾件事，再回 A 看「更多 → 最近動態」
15. **匯出 CSV**：記帳頁 →「⤓ 匯出 CSV」，用 Excel 打開看看
16. **手機版面**：瀏覽器按 `F12` → 左上角手機圖示 → 選 iPhone SE 與 iPhone 12/13，逐頁滑一次

試完把「哪裡不好用、哪裡看不懂、想多什麼」記下來，一次給我就好。

---

## 出問題怎麼辦

| 畫面上說 | 意思 / 怎麼處理 |
|---|---|
| 找不到 Node.js | 還沒裝，到 nodejs.org 裝 LTS 版，裝完重新雙擊 |
| 找不到 psql | PostgreSQL 的 `bin` 沒加進 PATH，照畫面指示加完重開 |
| npm install 失敗 | 多半是網路問題，換個網路再雙擊一次 |
| 連不上資料庫 | ① PostgreSQL 服務沒啟動 ② 密碼打錯 ③ **密碼裡有 `@` `:` `/` 這類符號**（改成純英數密碼最省事） |
| 開 localhost:3000 是空白或錯誤 | 看黑色視窗有沒有紅字；通常是上一項 |
| 想全部重來 | 雙擊 `3-重建示範資料.bat` |

卡住的話，把**黑色視窗的畫面截圖**給我就好，我看得出是哪一步。

---

## 手動指令版（如果你想自己控制）

```powershell
cd C:\couple-money
npm install
psql -U postgres -c "CREATE DATABASE couple_money_demo;"
# 建 .env（可複製 .env.example 改名），把密碼換掉：
#   DATABASE_URL="postgresql://postgres:你的密碼@localhost:5432/couple_money_demo"
#   DATABASE_URL_UNPOOLED="postgresql://postgres:你的密碼@localhost:5432/couple_money_demo"
npx prisma migrate deploy
npx tsx scripts/seed-demo.ts
npm run dev
```

---

## 這個版本是什麼

- commit：`b5d1157`（V1 功能 ＋ 韓系奶油風 UI）
- 測試狀態：Logic 114/114、Integration 233/233、E2E 全流程通過、tsc / eslint / build 皆通過
- `1/2/3-*.bat`、`scripts/seed-demo.ts`、這份說明是**只為了試用而附的，沒有進版控**，App 程式一行都沒改
- 全部只跑在你自己的電腦上，沒有任何雲端、部署或商業化設定
